No public/login.php validado da v8:

1. Logo após require auth.php, security.php já será carregado pelo novo auth.php.
2. Dentro do POST, antes de attempt_login(), adicione:
   verify_csrf();
3. Dentro do formulário, adicione:
   <?= csrf_field() ?>
4. Abaixo do botão Entrar, adicione:
   <a class="label" href="/forgot-password.php">Esqueci minha senha</a>

O novo src/auth.php deve ser integrado com as funções do src/security.php:
- login_is_rate_limited()
- record_login_attempt()
- bloqueio por locked_until
- must_change_password

O README traz a sequência de validação.
